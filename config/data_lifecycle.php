<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 記事データのライフサイクル設定
    |--------------------------------------------------------------------------
    |
    | RSS取得した記事データの保存期間とクリーンアップに関する設定
    |
    */
    'articles' => [
        // 保存期間（日数）
        'retention_days' => env('ARTICLE_RETENTION_DAYS', 90),

        // チャンク削除のサイズ（1回の削除クエリで処理する件数）
        'chunk_size' => 1000,

        // 安全上限（この件数を超える場合は警告）※将来の拡張用
        'max_deletions_per_run' => 100000,
    ],

    /*
    |--------------------------------------------------------------------------
    | RSS取得ログのライフサイクル設定
    |--------------------------------------------------------------------------
    |
    | RSS取得履歴ログの保存期間とクリーンアップに関する設定
    |
    */
    'logs' => [
        // 保存期間（日数）
        'retention_days' => env('LOG_RETENTION_DAYS', 90),

        // チャンク削除のサイズ（1回の削除クエリで処理する件数）
        'chunk_size' => 1000,
    ],
];

